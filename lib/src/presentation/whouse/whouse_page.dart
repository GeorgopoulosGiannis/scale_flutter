import 'package:fluent_ui/fluent_ui.dart';

class WhousePage extends StatelessWidget {
  const WhousePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ScaffoldPage(
      content: Text('Whouse'),
    );
  }
}
